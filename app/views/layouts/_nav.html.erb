<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container">
    <a class="navbar-brand">moody - a journalling app</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item">
          <%= link_to "Home", root_path, :class => 'nav-link'%>
        </li>

        <% if current_user %>
          <li class="nav-item">
          <%= link_to "Journal", journals_path(current_user.journal), :class => 'nav-link'%>
          </li>
          <% if current_user.journal %>
            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                Posts
              </a>
              <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                <%= link_to "New post", new_journal_post_path(current_user.journal.id), :class => "dropdown-item"%>
                <%= link_to "View posts", journal_posts_path(current_user.journal.id), :class => "dropdown-item"%>
              </div>
            </li>
          <% end %>
        <% end %>
      </ul>
      <ul class="form-inline my-2 my-lg-0 navbar-nav">
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            <% if current_user %> Hello, <%= current_user.name %> <% else %> User <% end %>
          </a>
          <div class="dropdown-menu" aria-labelledby="navbarDropdown">
            <% if user_signed_in? %>
              <%= link_to "Settings", edit_user_registration_path, :class => 'dropdown-item' %>
              <%= link_to "Log out", destroy_user_session_path, method: :delete, :class => 'dropdown-item'%>
            <% else %>
              <%= link_to "Log in", new_user_session_path, :class => 'dropdown-item' %>
              <%= link_to "Register", new_user_registration_path, :class => 'dropdown-item'%>
            <% end %>
          </div>
        </li>
      </ul>
    </div>
  </div>
</nav>